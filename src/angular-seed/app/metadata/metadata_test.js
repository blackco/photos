'use strict';

describe('myApp.metadata module', function() {

  beforeEach(module('myApp.metadata'));

  describe('metadata controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var metadataCtrl = $controller('metadataCtrl');
      expect(metadataCtrl).toBeDefined();
    }));

  });
});